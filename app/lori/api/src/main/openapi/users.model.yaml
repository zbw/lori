UserSessionRest:
  type: object
  required:
    - email
    - role
    - sessionId
  properties:
    email:
      type: string
    role:
      type: string
      enum:
        - read_only
        - read_write
        - admin
    sessionId:
      type: string

SessionIdCreated:
  required:
    - sessionId
  type: object
  properties:
    sessionId:
      type: string
